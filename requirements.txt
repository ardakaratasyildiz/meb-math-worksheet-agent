fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
google-genai>=1.0.0
python-dotenv>=1.0.0
requests>=2.32.0
chromadb>=1.5.0
sympy>=1.12
rank-bm25>=0.2.2
anthropic>=0.40.0
reportlab>=4.0.0
# Sprint 12-B: SVG render desteği (gorsel_geometri tipi için LLM-SVG çıktısı
# pdf_renderer.py içinde svglib ile reportlab Drawing'e çevrilir).
# Pin <1.6: svglib 1.6+ rlpycairo'ya bağlı → pycairo C build → Render Docker
# image'da libcairo2-dev yok, build fail. 1.5.x pure-Python implementation
# bizim ihtiyaç için (basit geometri + kısa ölçü label) yeterli.
svglib>=1.5.1,<1.6
lxml>=5.0.0
# Sprint 12-B / Phase C — salt_islem için LaTeX math notation rendering.
# matplotlib mathtext (LaTeX subset; \frac, \sqrt, ^, _, \sum vs.) PNG'ye
# çevirir; pdf_renderer.py reportlab Image flowable ile embed eder.
# Linux x86_64 binary wheel mevcut, system Cairo/LaTeX gerektirmez.
matplotlib>=3.8.0
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0.0
# Turso/libSQL — opsiyonel persistence backend (TURSO_DATABASE_URL set ise aktif).
# Set değilse db_connection.py klasik sqlite3'e fallback yapar.
libsql-experimental>=0.0.40
# P0 (billing ön koşulu): Clerk oturum JWT'sini sunucu-tarafı doğrulamak için.
# app/services/clerk_auth.py Clerk JWKS'inden RS256 imza doğrular → client'ın
# gönderdiği tenant_id'ye güvenmek yerine token'ın `sub` claim'inden üretir.
# [crypto] extra'sı RSA imza doğrulama (cryptography) getirir.
pyjwt[crypto]>=2.8.0
