# Çok-ders go-live (2026-07-10). Next.js production build'de (Vercel) okunur.
# Canlıda görünecek matematik-dışı dersler. Vercel dashboard'da aynı değişken
# set edilirse o değer BUNU override eder (dashboard > .env dosyası).
# Bir dersi geri kapatmak: bu listeden slug'ı çıkar + Render'da <DERS>_ENABLED=false.
NEXT_PUBLIC_ENABLED_SUBJECTS=fen,turkce,sosyal,ingilizce
